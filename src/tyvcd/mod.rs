/// Module containing the builder for the tyvcd IR format.
pub mod builder;
/// The specification of tyvcd IR format.
pub mod spec;
/// Module containing trait definitions to link elements with a trace path.
pub mod trace_pointer;
